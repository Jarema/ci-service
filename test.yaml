---
  commands:
    - name: show current dir
      cmd: pwd
    - name: list workspace
      cmd: ls -la
    - name: show current date
      cmd: date
    - name: pull docker image
      cmd: docker pull java
