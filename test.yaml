---
  commands:
    - name: show current dir
      sh: pwd
      cmd: notempty
    - name: list workspace
      sh: ls -la
    - name: some commands
      docker:
        image: java
        steps:
          - ls
          - pwd
